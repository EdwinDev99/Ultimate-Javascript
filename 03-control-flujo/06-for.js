// es igual que el while solo que aca se hace solo en una linea

// se compone de la inicializacion, comparacion y ejecutamos la accion(la actualizacion)

for (let i = 2; i < 10; i++) {
  if (i % 2 == 0) {
    console.log("numero par", i);
  }
}
